from setuptools import find_packages, setup

setup(
    name="PyDisc",
    package=find_packages(include=["PyDisc"]),
    version='0.1.0',
    description="A Python wrapper for connecting with the Discord API (under development)",
)
